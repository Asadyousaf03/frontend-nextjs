# Browser uses same-origin proxy (required: HospitALL org blocks public Cloud Run).
NEXT_PUBLIC_API_URL=/api/backend

# Upstream Cloud Run service (MedIQ project)
CLOUD_RUN_URL=https://genomic-ast-api-67343763423.us-central1.run.app

# Base64 of the genomic-ast-invoker service-account JSON (or raw JSON)
GCP_SA_JSON=
