# Copy to .env.local and adjust if port 8000 is already in use.
NEXT_PUBLIC_API_URL=http://localhost:8000

# If another app is using port 8000, start the backend on 8001 instead:
# uvicorn main:app --reload --port 8001
# NEXT_PUBLIC_API_URL=http://localhost:8001

# For production testing against Render locally:
# NEXT_PUBLIC_API_URL=https://backend-fastapi-4etx.onrender.com
